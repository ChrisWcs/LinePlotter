import glamorous from 'glamorous';

const Row = glamorous.div({
    display: "flex",
    flexDirection: 'row',
    justifyContent: 'center',
    alignContent: "center",
    marginTop: 30,
});

export default Row;